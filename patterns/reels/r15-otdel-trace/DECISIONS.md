# Decisions / ОТДЕЛ

Export correction: the first full pass was stopped with SIGINT after compiler metadata revealed hardcoded1080x1920 despite the storyboard1440x2560. The initial snapshot adapter output was also1080p and cropped the scaled logical layout. Corrected assemble.py viewport/root data-width/data-height to composition dimensions (legacy fallback retained). Rebuilt the exact storyboard and generated a new native1440x2560 snapshot at38seconds: verified the full third card, face, caption and frame edges visually. The authorized final pass uses unique v002 output. Never rely on storyboard dimensions alone: inspect compiler metadata and actual snapshot pixels before full render.

2026-09-13: user rejected FOLIO as not sufficiently developed and explicitly requested third type. Record selection TRACE without repeating style chooser. Preserve old candidate, reuse original media/transcript/crop/assets; rebuild actual graphics/actions in notebook visual language. Existing2K60higher-bitrate requirement retained. This request starts the build; it does not approve unseen TRACE frames for full MP4 export.

2026-09-13, later instruction: «сразу готовое видео жду». This expressly authorizes this TRACE Reel's immediate finished MP4 export without another frame-selection approval. storyboard.json and approval-state.json record fullExportAuthorized=true. This is a one-Reel exception, not a change to the global preview-first contract. Publication, cloud upload, bot activation and Git push remain unauthorized. Official renderer snapshots at 0, 20 and 38 seconds were generated before full export; the 20-second snapshot was visually inspected. Shared safe wrapper now reads storyboard FPS and optional bitrate while retaining legacy defaults; non-default exports require project encoded-media QA. One worker, at most four CPU cores and idle I/O priority remain enforced.
